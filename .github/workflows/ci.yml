name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  ci:
    uses: basidiocarp/.github/.github/workflows/rust-ci.yml@main
    with:
      workspace: true
      no-default-features: true
      audit: true
